#include"iostream"
using namespace std;
int main(){
    char content[4000], processed[5000]; int option;
    printf("enter option (1 for encode, 2 for decode): ");
    cin>>option;
    if(option==1){
        printf("enter string to encode: ");
        fgets(content, 4000, stdin);
        
    }
}
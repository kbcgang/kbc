#include <bits/stdc++.h>
using namespace std;
long long N,maxx1;
int main()
{
    freopen("bai5.inp", "r", stdin);
    freopen("bai5.out", "w", stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> a >> b;
    for (int i = 0; i < N; ++i) {
        cin >> num[i];
    }
}
